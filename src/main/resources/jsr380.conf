name            |type                   |required   |   constraintDesc
Null            |                       |false      |   格式：必须为空
NotNull         |                       |true       |
Size            |                       |false      |   长度：`${min:0}` ~ `${max:0x7fffffff}`
AssertFalse     |                       |false      |   值：必须为`false`
AssertTrue      |                       |false      |   值：必须为`true`
DecimalMax      |                       |false      |   值：最大`${value}`
DecimalMin      |                       |false      |   值：最小`${value}`
Digits          |                       |false      |   值：最大`${integer}`，小数点位数最大`${fraction}`位
Email           |                       |false      |   格式：邮箱
Future          |                       |false      |   值：大于当前日期
FutureOrPresent |                       |false      |   值：大于等于当前日期
Max             |                       |false      |   值：最大值为`${value}`
Min             |                       |false      |   值：最小值为`${value}`
Negative        |                       |false      |   值：负数
NotBlank        |                       |false      |   格式：不为空且不全为空格
NotEmpty        |                       |true       |   格式：不为空
Past            |                       |false      |   值：小于当前日期
PastOrPresent   |                       |false      |   值：小于等于当前日期
Pattern         |                       |false      |   格式：满足正则表达式`${regexp}`
Positive        |                       |false      |   值：正数
PositiveOrZero  |                       |false      |   值：正数或零
Length          |                       |false      |   长度：`${min:0}` ~ `${max:0x7fffffff}`